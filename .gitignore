# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/
build/
dist/

# Local secrets / env
.env

# On-disk GitHub API cache
.cache/

# Agent run artifacts (trajectories + generated eval tables are regenerated)
eval/results/*.jsonl
eval/results/*.md
!eval/results/.gitkeep

# OS / editor
.DS_Store
Thumbs.db

# Lokalny indeks CodeGraph (graf kodu per maszyna, odbudowywalny przez `codegraph init`)
.codegraph/
# Added by code-review-graph
.code-review-graph/

# Local Claude Code session notes (per-machine, not project documentation)
.claude/sessions/
